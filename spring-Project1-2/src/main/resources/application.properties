spring.application.name=spring-Project1-2

# --- PostgreSQL Connection Settings ---
# Note: Ensure you have created 'student_db' in pgAdmin!
spring.datasource.url=jdbc:postgresql://localhost:5432/student_db
spring.datasource.username=postgres
spring.datasource.password=Abhii@2310
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate Logic ---
# 'update' will automatically create/update your table based on the Student Entity
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Server Settings ---
# REST APIs usually run on 8080 by default
server.port=8080